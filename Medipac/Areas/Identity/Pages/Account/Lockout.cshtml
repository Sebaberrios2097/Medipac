@page
@model LockoutModel
@{
    ViewData["Title"] = "Cuenta Bloqueada";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-danger mt-5 mb-5">
                <div class="card-header bg-danger text-white text-center">
                    <h2 class="mb-0">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        @ViewData["Title"]
                    </h2>
                </div>
                <div class="card-body text-center">
                    <p class="text-danger fs-5">
                        <strong>Esta cuenta ha sido bloqueada temporalmente.</strong>
                    </p>
                    <p class="text-muted">
                        Por motivos de seguridad, el acceso a esta cuenta ha sido bloqueado. 
                        <br />
                        Intente nuevamente más tarde o comuníquese con el soporte si necesita ayuda inmediata.
                    </p>
                </div>
                <div class="card-footer text-center">
                    <a href="/" class="btn btn-primary">Volver al inicio</a>
                </div>
            </div>
        </div>
    </div>
</div>
